---
name: dianjing
description: 生成文章标题(公众号/知乎/小红书)。当用户要写文章标题、想标题、起标题、取标题、标题优化,或在写公众号/知乎/小红书/B 站内容前需要标题,以及做自媒体起号、写推文、发笔记时使用。输入一句话选题即可产出 10 条标题,分 6 类(玩梗体验/测评首发/工具实操/开源分享/思考心得/批判冲突金句),每条标注公式与钩子解读,并过一遍信息量自检和标题查重。不承诺流量,只保证具体、有立场、不撞车;写完正文要配标题时优先与 hualong 串起来用。
---

# AI 文章标题生成器(高传播爆款风格)

把一个选题一句话,变成一批能独立传播的标题。

公式来自 610 条真实标题 + 442 篇点赞数据的逆向分析。**先说清它做不到什么**:实测下来,标题的字数、有没有数字、用不用问号,跟最终点赞几乎没有相关性(最高的一个特征只有 0.09)。所以这个 skill 不承诺流量,它保证的是三件事:说清了具体的事、带明确立场、不和已有标题撞车。

核心价值:**标题本身就是一句可转发的金句**,不是干瘪的"浅谈/解析"。

## 何时用

用户出现以下任一情况即触发:

- "帮我想个标题 / 起个标题 / 这篇标题怎么写 / 标题优化 / 爆款标题"
- 在写公众号、知乎、小红书、B 站内容,卡在标题环节
- 做自媒体起号、发笔记、写推文前要定标题

## 输入

| 字段 | 必需 | 说明 |
| --- | --- | --- |
| 选题(一句话) | ✅ | 这篇要讲什么。如"用 Cursor 规则文件让 AI 按团队代码风格写" |
| 垂类 | 推荐 | 内容领域。缺省则从选题推断,或问一句 |
| 目标读者 | 推荐 | 如"1-5 年程序员 / 打工人 / AI 用户"。缺省则按选题推断 |

用户只给选题也能跑——垂类和读者从选题推断,但若出入大就补问一句。**不要因缺字段而拒绝产出**。

## 输出格式(固定)

一次性给 10 条,按**传播机制**配额,用表格:

```
| # | 标题 | 传播机制 | 公式类型 | 钩子解读(≤15字) |
|---|------|---------|---------|------------------|
| 1 | ... | 立场 | ⑥批判冲突金句 | ... |
| ... | | | | |
```

配额是硬性的:

- **5 种传播机制各 2 条**:信息差(你不知道但我查到了)、身份共鸣(说的就是我)、利益(能省什么 / 赚什么)、情绪(愤怒、惋惜、兴奋)、立场(我认为这是对的或错的);
- 其中**至少 2 条落在 ⑥批判冲突金句**上——它是实测里唯一在头部样本中明显更多的结构(Top29 占 13.8%,全量 1.5%);
- **公式类别不强制配比**。6 类公式只覆盖 38% 的语料,硬凑每类一条,凑出来的往往正是最平庸的那几条。

机制与公式的搭配参考:

| 机制 | 优先用的公式类型 |
| --- | --- |
| 信息差 | ②测评首发、③工具实操、⑤思考心得 |
| 身份共鸣 | ⑤思考心得、①玩梗体验 |
| 利益 | ③工具实操、④开源分享 |
| 情绪 | ①玩梗体验、⑥批判冲突金句 |
| 立场 | ⑥批判冲突金句、⑤思考心得 |

标题长度按平台显示约束来,不设统一硬上限:小红书 ≤20 字、公众号 ≤24 字、B 站 ≤25 字、知乎 ≤30 字。语料实际中位数是 28 字,**不要为了凑短牺牲信息量**。

末尾附两部分:

- 「**Top 3 推荐**」:各一句可判定的理由(信息差在哪、立场是什么、为什么会被点)。
- 「**查重**」:交付前跑 `scripts/title_overlap_check.py`,FAIL 的必须换掉再交。

## 6 类公式速览

详细公式、样本与高赞爆款公式在 `references/title_formulas.md`——**每次生成都先读它**,从里面挑公式套用。这里只放速查:

**① 玩梗 / 创意实验 / 体验揭秘**(传播力最强)

- `我用{工具}做了一部《{超级IP}》的{产物}…`
- `当我把{N}{内容}喂给 AI — 坏了,我成{X}了?`
- `{热点}到底能不能做出来?我的答案是:当然能。`
- `我潜伏进了"{诱惑}"的{场景},发现了一些灰色的秘密`

**② 测评 / 横评 / 首发观点**(时效冲量)

- `通俗易懂揭秘{新品},{碾压动词}{旧1}与{旧2}`
- `一手实测{新品} — {品类}的 ChatGPT 时刻到来了。`
- `{N}个动作,给{人群}准备的{品类}评测`
- `我花了{N}个小时,做了一个可能更{形容词}的{产物}`

**③ 工具实操 / 教学**(长尾沉淀)

- `从 0 到 1 带你速通{X}。`
- `从零开始,教你用{工具}搓出属于你自己的第一个{产物}`
- `零基础{人群}{时长}上手{X}的{N}步实操流程`
- `不会{门槛}也能{帅气结果},这是一份从 0 开始的{X}保姆…`

**④ 开源 / 分享**(招牌)

- `今天,我决定把{X}开源了。`
- `浪费{N}{单位}后,我开源了{X}`
- `这 {N} 个 {X} 太好用了`

**⑤ 思考 / 认知 / 心得**(立人设)

- `{经历}{时长}后,想跟你分享关于{X}的这{N}点心得。`
- `说一个反常识的真相,{X}`
- `观察了{N年},我把{对象}用{X}的水平分成了{N}个等级。`
- `{X}里最大的 Bug,也是{大词}最伟大的起点`

**⑥ 批判 / 冲突 / 金句**(头部样本里占比最高,每批至少 2 条)

- `{强者}的极致{负面词},正在{摧毁动词}我们的{Y}。`
- `{A}杀死了{X},也{绞杀}了{Y}。`
- `{事件}的全面战争,从{具体起点}开始。`
- `{产品}把{N}{人群},都变成了免费的"{比喻}"。`
- `{现象}里最大的 Bug,也是{大词}最伟大的起点`

还有一批史诗叙事、人物+神级、情感破防、苦工评测类公式,在 `references/title_formulas.md` 里。
这类结构的共同点是:**先有立场,再有信息**——没有判断的标题,再工整也只是描述。

## 8 条风格 DNA(套公式时不能丢)

1. **第一人称口语**:大量"我/你/还是想/说一个/聊聊/真觉得"——像聊天,不像通稿。
2. **数字只用来具体化,不是加分项**:实测里,含数字的标题在高赞组(47.8%)和其他组(47.7%)几乎一样多。有真实数字就用,没有就别编,更别为凑数字硬塞。
3. **具象动作动词**:搓 / 喂 / 还原 / 速通 / 拆解 / 降维打击 / 揭秘 / 挖出 / 潜伏 / 偷 / 蒸馏。禁用"浅谈/解析/一文读懂"当万能前缀。
4. **代价 / 反差 / 反预期前置**:"浪费 20 亿 Token 后""坏了我成数字生命了""我劝你先别急着用 AI""说一个反常识的真相"——把勾子放最前。
5. **标题 = 可独立传播金句**:不点开正文也想转。如"AI 里最大的 Bug,也是人类文明最伟大的起点"。
6. **个人判断定调**:"这是我觉得现在最好用的…""我答案是:当然能""我觉得这就是…有史以来最大的…"。
7. **反差 / 反转结构**:用"居然是 / 反而 / 也是…的起点"制造认知落差。
8. **立场要亮出来**:断言、批判、警告类结构在头部样本里明显更多(Top29 占 13.8%,全量 1.5%)。同一篇内容,敢下判断的标题比中性描述更值得试。

## 生成流程

0. **先确认有没有正文、有没有 title-brief**:
   - 有 `title-brief`(hualong 的交接物,格式见 `references/handoff.md`)→ 直接用它,不必通读全文;
   - 只有正文 → 先读一遍,自己整理出钩子、数字、立场;
   - 都没有 → 先定选题角度,并告诉用户"标题先行,正文后续必须兑现"。
   无论哪种情况,**交付前都要用 `scripts/promise_check.py` 核验 Top3 标题**:标题里的数字必须 100% 出现在正文,关键词覆盖率不低于 45%。
1. **读 reference**(两份,分工不同):
   - `references/title_formulas.md`(**每次必读**):6 类公式 + 高赞爆款公式 + 风格 DNA + 样本 + 生成 Prompt。主弹药库,体量小。
   - `references/title_index.md`(**按需读**):610 条标题索引(顶部"按赞 Top30" + 全量按时间倒序,**只有标题**,没有日期/赞数/链接)。要看具体措辞时用 grep 或跳读挑相关部分,不要整份读进上下文(44KB)。
   - `references/handoff.md`(**有正文时必读**):与 hualong 的交接契约,title-brief 的字段格式和验收线都在这。
2. **解析选题**:抽出可填的槽——{工具}/{人群}/{产物}/{反差点}/{数字素材}。有真实数字就用,没有就用模糊表达(大概 / 不到),**不要为了具体去编一个数字**。
3. **按机制套公式**:5 种传播机制各 2 条,共 10 条。每条标"传播机制 + 用了哪个公式 + 钩子解读"。
4. **自检**(过不了就改):
   - **信息量检查(逐条过)**:有没有具体的人 / 物 / 动作 / 场景?说不出具体对象就是空话,重写。
   - **立场检查**:这条标题有没有判断?全是中性描述就换动词或补观点。
   - **可转述检查**:不点开正文,这句话单独发出来还成立吗?不成立就重写。
   - **疑问句不限也不加分**:实测里疑问句在高赞组占 4.3%、其他组 3.3%,没有差别。想设问就设问,别为躲问号把话说别扭。
   - 有没有"浅谈/解析"等书面词?删掉换具象动词。
   - 像本人随口说的,还是像通稿?像通稿就加口语/反差。
   - 每条都要通过 `scripts/title_overlap_check.py`:连续重合 ≥10 字判 FAIL,必须换句式或换角度重写,只改标点不算。
   - Top3 要过 `scripts/promise_check.py --title "…" --body 正文.md`:标题里的数字必须能在正文找到,关键词覆盖率 ≥45%(65% 以下记提醒)。
5. **挑 Top 3**:从 10 条里选 3 条,各给一句可判定的推荐理由。如果 `scripts/title_feedback.py --report` 里某类机制已经攒到 3 条以上数据,按它排序,而不是凭感觉。
6. **发布后回填**(可选但强烈建议):发出去 3 天后跑一次
   `py -3 scripts/title_feedback.py --add --title "标题原文" --platform 公众号 --category "⑥批判冲突金句" --mechanism 立场 --impressions 12000 --clicks 900 --likes 120`。
   攒够 10 条再跑 `--report`,你就有了自己账号的标题基线,而不是借用别人的。

## 示例

**输入**:选题"用 Cursor 规则文件让 AI 按团队代码风格写" / 垂类"AI 编程" / 读者"1-5 年程序员"

**输出**(节选):

| # | 标题 | 公式类型 | 钩子解读 |
| --- | --- | --- | --- |
| 1 | 我只加了 1 个文件,AI 就再没写崩过团队代码风格 | ①玩梗体验 | 1 个文件的反差 |
| 2 | 一手实测 Cursor 规则文件 — AI 按你风格写的 ChatGPT 时刻 | ②测评首发 | 一手实测定调 |
| 3 | 从 0 到 1 带你速通 Cursor 规则文件。 | ③工具实操 | 速通+明确产物 |
| 4 | 浪费 3 亿 Token 后,我开源了团队代码风格的规则模板 | ④开源分享 | 代价前置+开源 |
| 5 | 说一个反常识的真相,AI 写不对你代码,怪你没写规则 | ⑤思考心得 | 反常识真相 |

**Top 3**:①(反差最强)、③(最可复制,长尾)、④(代价前置+开源招牌)。

## 数据基线与已知局限

这个 skill 的结论来自两批数据,写在这里是为了让后续修改有据可依:

| 数据 | 规模 | 用途 |
| --- | --- | --- |
| 标题索引 | 610 条(去重) | 公式提炼、真实措辞参考、查重底库 |
| 实时赞数样本 | 442 篇(带发布时间) | 特征与点赞的相关性分析 |

结论分三档,别混着用:

1. **可以当规则用的**:冲突 / 立场类结构在头部样本里占比明显更高(Top29 的 13.8% vs 全量 1.5%);标题必须说清具体对象和立场;不许撞车。
2. **没有证据、别当规则的**:标题长度(全量中位 28 字,和点赞相关性 0.046)、有没有数字(相关性 0.025)、用不用问号(高赞组 4.3% vs 其他组 3.3%)。这些东西可以用来适配平台显示,但不能拿来预测效果。
3. **还没验证的**:公众号和小红书的基线;非 AI 垂类;账号权重、话题热度、发布时间的影响。想验证就按同一套方法采集新样本重测。

## 边界

- 数字要**合理真实**,别编离谱的(如"浪费 9999 亿")。数字负责具体,不负责提高点击。
- 不替用户决定选题对错,只把给定选题做成最优标题。
- 用户若说"换一批",就换 5 类里没用过的公式重产;若说"只有某一类",就专攻那类多给几条。
- 用户垂类非 AI/技术时(如美食、育儿),公式同样适用,把变量换成该领域词即可——这套是结构,不绑死 AI。
- **不承诺流量**。标题只影响被点开的概率,曝光由平台、账号和时间决定。
- 写完正文要配标题时,先让 `hualong` 出正文,再回来定标题——标题必须兑现正文里的钩子,不能互相打架。
